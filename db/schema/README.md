# Database Schema

This folder contains all SQL schema definitions (DDL).

Rules:
- All tables must be defined here
- SQL must be executed manually via phpMyAdmin (XAMPP)
- No auto-migrations
- One change = one SQL file
